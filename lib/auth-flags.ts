/**
 * When `true`, credentials sign-in is email-only (no password check), and the login UI
 * hides password, forgot-password, and Google. Set to `false` to restore full auth.
 */
const SIMPLE_AUTH_MODE = true;

export { SIMPLE_AUTH_MODE };
