/**
 * When `true`, credentials sign-in is email-only (no password check), and the login UI
 * hides password, forgot-password, and Google. Set to `false` for email + password auth.
 */
const SIMPLE_AUTH_MODE = false;

export { SIMPLE_AUTH_MODE };
